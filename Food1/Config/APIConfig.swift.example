//
//  APIConfig.swift.example
//  Food1
//
//  TEMPLATE FILE - Copy this to APIConfig.swift and add your actual values
//  DO NOT commit APIConfig.swift to git (it's in .gitignore)
//

import Foundation

struct APIConfig {
    /// Your Cloudflare Worker endpoint URL
    /// Format: https://food-vision-api.YOUR_USERNAME.workers.dev/analyze
    static let proxyEndpoint = "YOUR_CLOUDFLARE_WORKER_URL_HERE"

    /// Authentication token for iOS app → proxy communication
    /// Must match the AUTH_TOKEN set in your Cloudflare Worker secrets
    /// Generate a random UUID or strong password
    static let authToken = "YOUR_AUTH_TOKEN_HERE"
}

// SETUP INSTRUCTIONS:
//
// 1. Copy this file to APIConfig.swift in the same directory:
//    cp APIConfig.swift.example APIConfig.swift
//
// 2. Open APIConfig.swift and replace:
//    - YOUR_CLOUDFLARE_WORKER_URL_HERE with your deployed worker URL
//    - YOUR_AUTH_TOKEN_HERE with the token you set in Cloudflare secrets
//
// 3. Add APIConfig.swift to your Xcode project (if not already added)
//
// 4. Verify APIConfig.swift is in .gitignore (it should be!)
//
// 5. Build and run the app
//
// For proxy setup instructions, see: proxy/food-vision-api/README.md
